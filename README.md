# homebrew-tap

Personal Homebrew tap for CLI tools.

```bash
brew install christianteohx/tap/calctl
```

## Available formulae

- `calctl` — native macOS Apple Calendar CLI
- `skill-dep-fixer` — check and fix OpenClaw skill dependencies
